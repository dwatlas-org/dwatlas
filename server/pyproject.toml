[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "sqlmodel>=0.0.39",
]

[tool.fastapi]
entrypoint = "api.main:app"

[dependency-groups]
dev = [
    "prek>=0.4.14",
    "pytest>=9.1.1",
]
